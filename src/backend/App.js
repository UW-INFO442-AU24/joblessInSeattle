import React from "react";
import SleepTracker from "../frontend/sleepTracker";

const App = ({ auth }) => {
    return (
        <div>
            <SleepTracker></SleepTracker>
        </div>
    );
};

export default App;